<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ url_for('public.index') }}">📋 Docházka SSPU</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      {% if current_user.is_anonymous() %}
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="{{ url_for('auth.login') }}">
              <i class="bi bi-box-arrow-in-right"></i> Přihlásit se
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ url_for('auth.register') }}">
              <i class="bi bi-person-plus"></i> Registrovat
            </a>
          </li>
        </ul>
      {% else %}
        <ul class="navbar-nav me-auto">
          {% if user.access == "A" or user.access == "B" %}
            <li class="nav-item">
              <a class="nav-link" href="{{ url_for('auth.upload') }}">
                <i class="bi bi-upload"></i> Nahrát XML
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ url_for('auth.newmonth') }}">
                <i class="bi bi-calendar-plus"></i> Vytvořit měsíc
              </a>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarAddDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="bi bi-plus-circle"></i> Přidat
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarAddDropdown">
                <li><a class="dropdown-item" href="{{ url_for('auth.groups') }}">Skupinu</a></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.timecards') }}">Čtečku</a></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.user_add') }}">Nového uživatele</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.addToGroup') }}">Uživatele do skupiny</a></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.timecardForGroup') }}">Čtečku ke skupině</a></li>
              </ul>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarViewDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="bi bi-eye"></i> Zobrazit
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarViewDropdown">
                <li><a class="dropdown-item" href="{{ url_for('auth.show_groups') }}">Skupiny</a></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.show_timecards') }}">Čtečky</a></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.show_userGroups') }}">Uživatele ve skupinách</a></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.groupTimecards') }}">Přístup k čtečkám</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.user_list') }}">Seznam uživatelů</a></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.mesicni_vypis_vyber') }}">Výpisy uživatelů - stravenky</a></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.mesicni_vypis_vyber_hodiny') }}">Výpisy uživatelů - hodiny</a></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.pristupy_all') }}">Všechny přístupy</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.pristupy') }}">Moje přístupy</a></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.skupiny') }}">Moje skupiny</a></li>
                <li><a class="dropdown-item" href="{{ url_for('auth.vypisy') }}">Výpis po měsících</a></li>
              </ul>
            </li>
          {% else %}
            <li class="nav-item">
              <a class="nav-link" href="{{ url_for('auth.pristupy') }}">Moje přístupy</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ url_for('auth.skupiny') }}">Moje skupiny</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ url_for('auth.vypisy') }}">Výpis po měsících</a>
            </li>
          {% endif %}
        </ul>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="{{ url_for('auth.account') }}">
              <i class="bi bi-person-circle"></i> {{ user.username }}
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ url_for('auth.logout') }}">
              <i class="bi bi-box-arrow-right"></i> Odhlásit se
            </a>
          </li>
        </ul>
      {% endif %}
    </div>
  </div>
</nav>
